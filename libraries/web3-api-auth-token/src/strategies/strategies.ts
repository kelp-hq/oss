/**
 * Possible Strategies.
 * The enum values should be kept max 4 characters. There is no limitation but less is better.
 * @public
 */
export enum IAuthStrategy {
  /**
   * Substrate strategy.
   */
  'substrate' = 'sub'
}
